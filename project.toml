# project.toml — Machine-readable project manifest for AI agents
# Auto-generated by gen-project-toml.sh — edit freely, won't be overwritten

[project]
name = "sui-mcp-server"
description = "MCP server for Sui blockchain — wallet management, token ops, transactions, Move calls, staking"
stack = ["node", "typescript"]
entry = "src/index.ts"
last_commit = "2026-03-27"

[commands]
build = "npm run build"
test = "npm test"
dev = "npm run dev"

[agent]
has_claude_md = false
notes = ""
